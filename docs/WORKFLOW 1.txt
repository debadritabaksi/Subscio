PROBLEM STATEMENT 4:
Signals Harvesting Engine - Agentic AI Workflow System
Industry Domain
Lead Intelligence | AI Automation | Sales Intelligence
Problem Statement
Businesses lose opportunities because they fail to capture and analyze buying signals, hiring signals, creator signals, market shifts, and business intent data in real-time.
Sales and growth teams manually monitor:
	LinkedIn activities,
	funding announcements,
	hiring posts,
	social signals,
	website changes,
	and engagement patterns.
This process is extremely inefficient.
Challenge
Build an AI-powered Signals Harvesting Engine that:
	captures business signals automatically,
	analyzes intent,
	prioritizes opportunities,
	and triggers automated workflows.
Agentic Al Workflow
Step 1 - Signal Collection Agent
Collect signals from:
	LinkedIn,
	Twitter/X,
	websites,
	job portals,
	funding news,
	and social activity.
Step 2 - Intent Analysis Agent
The AI identifies:
	hiring intent,
	buying intent,
	partnership opportunities,
	creator collaborations,
	and business expansion signals.
Step 3 - Prioritization Agent
The system scores:
	lead quality,
	urgency,
	engagement level,
	and conversion potential.
Step 4 - Automation Agent
Automatically:
	send outreach emails,
	generate follow-up reminders,
	create CRM entries,
	and trigger workflows.
Step 5 - Intelligence Dashboard
Generate:
	lead intelligence dashboards,
	opportunity heatmaps,
	and conversion analytics.
Expected Features
	Signal monitoring system
	Al lead scoring
	Automated workflows
	CRM integrations
	Opportunity intelligence dashboard
	Agentic automation pipeline







 WORKFLOW 1


1. phase 1 - login/registration : user registers through their company gmail id 
             dashboard : here for the first time the users sees a configure workplace card in the command center, for pipeline tab if without giving user company details user goes there then it should have an option the arrow hich will be directing user back 2 to the command centre similar for compilance ledger if user goes there without registering the company details then also they have to come back to the dashboard and first register their Company details like from the workplace configuration card After that After the user has given the company details 
next comes this question Then in the company details Reuser is supposed to keep their company Industry type and give a description of their company a very short description based on which the ai the agent ai this is the first agent ai that our website will have which will be using the details entered by the user company user about their company and then analysing what type of company the user has based on that it will figure out that this can be the targeted target companies of my user And give the list to the user back to the user that these are the target companies that you should be having It will be an option yes a user can add later on but this will be primarily done by the first ai agent That is searching the web the different resources the rss text crunch and Apis and listing all the tracking companies of my user Now based on the details I was thinking of going the URL way That the user gives the companies url and the first AI agent analyses the company url gathers all the information possible of the of the company entered by the user and then gives the target company list to the user back to the user based on which The next workings are done.
 phase 2 - The signal harvestor agent starts working from here. Based on the details fetched by the first agent about the users company The first agent will store the data in the database that these are the target companies for my user then the signal harvested agent should collect those data from the database and then scrap the other websites that is different websites tech crunch rss apis and other websites that you have mentioned And gather all the data required for those companies if there are any hot leads warm leads or cold leads and Blake basically correct the data what are they doing funding new things actually the leads thing doesnt come here it only it is only signal collecting that is if it is the fundamental hiring funding new Perhaps projects are searching for new job locations or anything It will scrap the data The audit from those websites about the target companies that they are doing this currently they are doing that which is really which is then Send to the next agent that is the internal analyzer agent. If the user has entered our website for the first time then the signal collected agent should also look at the historical details of the past 30 days And figure out whatever post the target companies have made they collect all the data from the post made by the target companies If it is funding if it is hiring if it is looking for a new workplace anything Related to my company. The data collected by the signal harvester is in the different companies post and everything should be shown in the live signal panel that is there on the left side of my website For the first time the user is doing it for the first time then it will show that previous the on which date the data has been posted if it is for previous for the current once just show it as a live signal as the logic is right now but for the previous one like the historical data it should show the date on which the data has been given by the target companies.phase 3- Accessing the same database the third agent that is the intent analyzer agent will be scanning all the data collected by the signal harvester agent and see which company has a hiring intent or a buying intent or a partnership opportunity create a collaboration opportunity intention And then it will be analysing the intense based on the recent posts of those companies as given by the signal harvester to the database That will be the job of the internal analyst that is only explaining or understanding what is my target companies intent right now based on their post
Phase 4 - Here comes our lead score agent that is the 4th agent The purpose of this agent is to calculate the lead score of the signals harvested and the intents like the intent after intent analysis this signal this agent will be Rang to rank them according to the lead scoreboard will use a formula define formula which will Be used to predict which is a hot lead which is a warm lead and which is a cold lead Based on the urgency the emergence of the post and also suppose a company is hiring a new intern who is going to be and marketing head In the in my target company But then there is another company which is going to invest $10 million in the Indian market So I can target that company Being Nayika and giving this example That instead of targeting The company which is hiring a new marketing head that should be a warn lead the hot lead should be the one who is wanting to invest money in the Indian market and my platform can provide them the space where they can through which they can enter the Indian market system.

phase 5 - Next come the corsair agent or the agent which will be writing the pitches based on the companies I want which is hot leads The so I want this agent to even prepare a pitch For the cold lead That is any leads which are coming up on my charts okay the corsair agent should have a pitch ready for me and when I tap on the company details from the dashboard it should show me the pitch. Also the allow button should be here if I tap on the allow button that is the user taps on the allow button then it takes them to the email page directly where the user can send the email to the target company which has been prepared by the agent Which contains all the necessary details required for the pitch also mentioning why the target company should choose my company

phase 6 - Intelligent dashboard Agent
will figure out the coversion potentionl,heatmaps and lead interst as described below. thid id the workflow

Section 1: Phase 1 & Agent 1 (The Context & Discovery Agent)UI Navigation Protection GuardrailsTo prevent a user from exploring an empty dashboard before configuring their business context, your Next.js application will leverage a centralized React Context state hook combined with conditional route blockers.When a user authenticates via Google OAuth, the system checks for an existing profile record in the database. If none is found, the global state key isConfigured defaults to false.If the user attempts to access the Pipeline or Compliance Ledger view paths, your layout file routes them back to the primary workspace view using a clean Next.js router instruction (router.push('/dashboard')). The interface simultaneously displays a prominent, high-contrast center dashboard card labeled "Configure Workspace Context" along with a clear visual warning banner indicating that workspace settings must be completed first.Minimalist Inputs & Magic AI Analysis (The No-URL Route)You do not need to rely on forcing the user to find and paste in long lists of target company web URLs. Instead, the user inputs a simple, plain-text string detailing their business footprint inside a single workspace setup container.For Nykaa, the input text is:"We are Nykaa, a premier B2B beauty and cosmetics marketplace in India. Our goal is to locate, score, and onboard global cosmetic brands, clean skincare startups, and international wellness labels looking to distribute or launch exclusively within the Indian retail ecosystem."The Context & Discovery Agent accepts this raw string input and parses it autonomously. This minimizes data entry fatigue for the user while allowing your backend to construct highly accurate lookup queries under the hood.Technical Competitor BenchmarkingTop enterprise search suites like Apollo.io, Salesforce Sandbox platforms, and Clay tables enforce strict structured setups during onboarding to ensure lookups remain relevant. To match this quality, your configuration card will contain three minimalist, self-parsing input parameters:Ideal Customer Profile (ICP) Target Scale: A dropdown menu specifying company size constraints (e.g., Emerging D2C Startups, Mid-Market Global Labels, Enterprise Cosmetic conglomerates).Geographic Scoping Boundary: Text input array field specifying target regions (e.g., ["North America", "Europe", "APAC"]).Primary Value Proposition Tags: Multi-select element mapping out what your company offers to leads (e.g., ["Distribution Infrastructure", "Exclusive E-Commerce Launchpad", "Regulatory Compliance Clearances"]).Manual Target Entry OverridesIf a user (e.g., a Nykaa partner acquisition lead) has a specific company in mind that they want to track immediately—such as Fenty Beauty, which may be exploring entry vectors into India—they can type the name directly into a manual entry box within the workspace interface. Your system handles this input by bypassing the discovery loop and saving it directly to your core PostgreSQL target accounts table as a high-priority profile.Technical Workflow: Agent 1 (Context & Discovery Engine)[Raw Profile String Input] ──► FastAPI Controller ──► OpenAI-Compatible LLM (Function Calling)
                                                            │
                                                            ▼
                                                Extract Semantic Query JSON
                                                            │
                                                            ▼
                                              Parallel Public API Discovery Search
                                              ├── Google Custom Search API
                                              ├── TechCrunch / RSS Feeds
                                              └── Crunchbase Data Queries
                                                            │
                                                            ▼
                                                PostgreSQL Database Write
The user types their company description into the UI and clicks Save Configuration.The frontend fires an asynchronous HTTP request (POST /api/v1/context/initialize) sending the text package to your FastAPI backend server.The backend routes this payload to services/ai_service.py. The system passes the text into an OpenAI-compatible LLM execution wrapper using structured tool calling mechanisms.The LLM system prompt forces the model to interpret the company definition and return a structured JSON mapping out key lookup entities:JSON{
  "user_company_type": "B2B E-Commerce Marketplace",
  "extracted_target_keywords": ["cosmetic brands", "skincare labels", "indie beauty"],
  "semantic_search_queries": ["international cosmetics expanding globally", "D2C beauty brands funding"]
}
The backend reads the generated semantic_search_queries array and executes parallel asynchronous HTTP lookup requests via an asynchronous HTTP client package across your search engine integrations:Google Custom Search API: Queries search indexes for market entrants.TechCrunch/Financial RSS Feeds: Scans news structures for recent funding rounds.Crunchbase / Open Corporate Directories: Filters labels matching the target profile.The discovered company profiles (including any manually added entries like Fenty Beauty) are saved to your relational database tables using an inverted state transaction block to verify uniqueness.The database updates are passed back to the user interface as a verified list of discovered targets.Section 2: Phase 2 & Agent 2 (The Signal Harvester Agent)Historical vs. Real-Time Data MappingWhen a target company is registered in your database for the first time, your system experiences a Cold Start. If the dashboard is left completely blank, it feels broken to the user. To solve this, the Signal Harvester Agent handles data aggregation across two distinct velocity patterns:The Day-Zero Backfill (Historical Data Pass): The moment an account profile is activated, a background worker triggers an audit sequence that scrapes historical records from the past 30 days. These items are saved to the database with their original historical timestamp strings and pushed to the left-hand live stream ticker panel. The UI maps an explicit historical marker next to them (e.g., [Historical: June 14, 2026]).The Live Radar Ticker Stream (Real-Time Ingestion Pass): For ongoing monitoring, cron-scheduled system routines pull updates from active endpoints every 15 minutes. Any incoming data element is processed instantly and pushed to the top of the interface ticker as a pulsing, real-time alert badge labeled LIVE.The Scraping Solution: Safe Data Ingestion without Getting BannedScraping modern social media walls (like LinkedIn or Instagram) via headless browsers (Puppeteer/Selenium) will quickly result in your hackathon server's IP address getting blocked or hitting CAPTCHA firewalls. To bypass this restriction safely during your presentation, use a clean, production-grade data engineering split:Public Open Feeds and Developer APIs: Rely on open, unblocked data networks such as the GitHub Events API (for monitoring engineering activity spikes), TechCrunch RSS Feeds via the Python feedparser library, and public Applicant Tracking System (ATS) endpoints (like Greenhouse and Lever job board boards), which are fully public and do not enforce bot blockers.The Simulated Developer Webhook Console (The Demo Asset): For protected data sources (like closed LinkedIn profiles or paid ZoomInfo streams), utilize the Webhook Simulation Console built into your FastAPI backend (backend/app/routes/webhooks.py). This endpoint accepts structured JSON mock objects that mimic real enterprise webhook arrivals. This provides a controlled testing layout for the judges while keeping the codebase production-ready.Technical Workflow: Agent 2 (Signal Harvester Engine)[Target Accounts Database Check] ──► Celery Beat (15-Minute Interval Execution Loop)
                                                │
                                                ▼
                                    Parallel Fetch Workers
                                    ├── HTTP Async Client (Greenhouse/Lever)
                                    ├── Feedparser Worker (RSS / News Streams)
                                    └── Emitter Ingestion Route (Simulated Inputs)
                                                │
                                                ▼
                                    Idempotency Encryption Hash Check
                                                │
                                                ▼
                                    PostgreSQL / Live Ticker Push
A system-wide background task engine (Celery Beat backed by a Redis message queue) triggers an invocation execution script every 15 minutes.The worker script reads the list of active tracked corporate identities inside your database.The task system runs parallel HTTP requests across active discovery directories:Job Boards: Queries [https://api.greenhouse.io/v1/boards/](https://api.greenhouse.io/v1/boards/){target_brand}/jobs to find open positions.News Networks: Parses technology syndication feeds via the Python feedparser utility.Simulated Inputs: Ingestion endpoints parse mock events from the web user dashboard.As raw signal payloads are harvested, your code builds a unique, cryptographic verification identification string using SHA-256 formatting based on the text payload:Pythonimport hashlib
dedup_hash = hashlib.sha256(f"{company_name}_{signal_title}_{signal_body}".encode()).hexdigest()
The system runs a database lookup against this dedup_hash. If a match is found, the entry is dropped to prevent data duplication. If unique, the data is saved as an un-analyzed row in your database.The backend triggers a WebSockets layer push or a React state refresh on the frontend, smoothly displaying the new card at the top of the LiveRadarTicker.tsx feed view.Section 3: Phase 3 & Agent 3 (The Intent Analyzer Agent)Intent Identification MechanicsThe Intent Analyzer Agent acts as an analytical filter layer that converts raw text logs into actionable commercial opportunities. It monitors and classifies four primary business intent signatures:Hiring Intent: Looks for active job listings for executive leaders or specialized teams (e.g., Fenty Beauty posting for a "Head of Indian Logistics" indicates an immediate regional launch plan).Buying Intent: Detects software stack transitions or procurement inquiries (e.g., a brand searching for enterprise warehouse space or localization software tools).Partnership / Expansion Readiness: Scans news networks for corporate declarations or international trade registrations (e.g., a label announcing an APAC market expansion push).Creator Collaboration Signals: Monitors marketing budget expansions or leadership updates inside brand management units.Expanded Analytical System CapabilitiesTo maximize the platform's value, the Intent Analyzer Agent tracking framework also includes Technology Stack Drift Tracking. By monitoring technical data footprints (such as changes in public code repositories, domain DNS settings, or specialized engineering hires), the AI detects if a target brand is changing its infrastructure (e.g., migrating its storefront from basic Shopify setups to advanced headless architectures). This signal indicates they are scaling up and need enterprise-grade marketplace distribution.Technical Workflow: Agent 3 (Intent Analyzer Engine)[Unscored Database Log Ingest] ──► SQL Alchemy Query Check ──► LLM Parsing Sandbox
                                                                        │
                                                                        ▼
                                                            System Prompt Validation
                                                                        │
                                                                        ▼
                                                            Extract Categorized Intent
                                                                        │
                                                                        ▼
                                                            Database Update & Status Flag
An asynchronous background job runs a database query to find rows where intent_score is marked as None (indicating they are raw, un-analyzed signals).The text content, company profile metadata, and target titles are bundled into a payload and sent to the LLM interface layer via services/ai_service.py.The agent processes the data using a specialized analytical prompt framework:PlaintextYou are an expert enterprise business intelligence analyst. Your task is to read the raw business signal text and determine the primary strategic intent of the company.
Classify the text into one of these exact string categories: [hiring, buying, expansion, general].
Provide a brief, single-sentence strategic reasoning string outlining the corporate goal.
The model parses the input text and maps the output into a strict data validation container (Pydantic Schema) to guarantee structural format consistency.The backend records the updated metrics directly back to the database row, setting categorical parameters like intent_stage to values such as consideration or purchase_ready.The platform updates the dashboard table UI, replacing empty indicators with clear, color-coded intent badges.Section 4: Phase 4 & Agent 4 (The Score Calculator Agent)The Weighted Score Calculation MatrixTo determine which leads deserve immediate attention, the Score Calculator Agent applies a strict, deterministic scoring matrix. The formula combines the intent stage weight, data source authority, and real-time urgency metrics into a final score from 1 to 100:$$S = \text{clip}\left( \omega_{\text{stage}} \cdot M_{\text{stage}} + \omega_{\text{source}} \cdot A_{\text{source}} + \omega_{\text{recency}} \cdot R(t) + \delta_{\text{bonus}}, \, 1, \, 100 \right)$$Component Definitions$M_{\text{stage}}$: The Intent Stage base multiplier weight ($\text{Purchase Ready} = 50$, $\text{Consideration} = 35$, $\text{Awareness} = 20$, $\text{Targeting} = 10$).$A_{\text{source}}$: The data source authority anchor value ($\text{Official Government/ATS Portals} = 30$, $\text{Financial Press} = 25$, $\text{General News Feeds} = 15$).$R(t)$: A linear time-decay function calculating signal recency over a given timeframe:$$R(t) = \max\left(0, \, 20 - \lfloor \Delta t_{\text{hours}} \rfloor\right)$$$\delta_{\text{bonus}}$: An active target modifier bonus (+10 if the signal matches an explicit user target watch list element).The system weights are balanced to equal 1.0 ($\omega_{\text{stage}} = 1.0$, $\omega_{\text{source}} = 1.0$, $\omega_{\text{recency}} = 1.0$). The final output value is clipped strictly between 1 and 100.Comparative Calculation Walkthrough (The Nykaa Scenario Match)Scenario Alpha: Minor Team Internship HireThe Signal: Glossier posts an application for a summer graphic design intern on a public channel.The Math Calculation:$M_{\text{stage}}$ (Awareness) = 20$A_{\text{source}}$ (General Job Post) = 15$R(t)$ (Posted 2 hours ago) = 18$\delta_{\text{bonus}}$ (Not on core priority target watch list) = 0Final Score ($S$): $20 + 15 + 18 + 0 = \mathbf{53}$ (Muted/Warm Lead — Low priority status)Scenario Beta: Major Regional Infrastructure LaunchThe Signal: Fenty Beauty announces a $10M capitalization allocation for APAC distribution supply chains on a financial news wire.The Math Calculation:$M_{\text{stage}}$ (Purchase Ready) = 50$A_{\text{source}}$ (Financial Press Wire) = 25$R(t)$ (Ingested 1 hour ago) = 19$\delta_{\text{bonus}}$ (Matches core manual priority target override tracker) = 10Final Score ($S$): $50 + 25 + 19 + 10 = 104 \rightarrow \text{clipped to } \mathbf{100}$ (Hot Lead — Immediate Priority Alert)Defining Conversion PotentialAs outlined in your hackathon problem statement, Conversion Potential measures how well a target lead's current business pain points align with your specific corporate advantages.For example, if Fenty Beauty wants to launch in India but lacks local warehouses, and your company (Nykaa) already owns specialized cosmetics supply chains, the alignment score peaks. The system identifies this match and flags the lead on your dashboard with a high conversion potential rating.Autonomous Pitch OrchestrationThe pitch creation process runs directly inside your backend core service layer (services/ai_service.py). It uses a structured prompt that takes the lead's raw signals, combines them with your corporate context vectors, and outputs a tailored outreach email.Because the generation logic loops in parameters like your active fulfillment options, the output draft explains exactly why partnering with Nykaa will solve their specific regional distribution bottlenecks.Technical Workflow: Agent 4 (Score Calculator Engine)[Intent Enrichment Update Event] ──► Read Parameters Matrix ──► Execute Python Scoring Function
                                                                            │
                                                                            ▼
                                                                Evaluate Conversion Potential
                                                                            │
                                                                            ▼
                                                                Check Priority Threshold
                                                                ├── IF S < 90: Save Row & Update UI
                                                                └── IF S >= 90: Trigger AI Pitch Generation
A database update trigger activates the calculation script as soon as Agent 3 completes intent mapping for a signal.The backend scoring script reads the signal metrics matrix and executes the weighted evaluation formula.The system calculates the Conversion Potential index by running a text vector match between your company's profile description and the target lead's extracted pain points.The generated scores are written to the database row fields (intent_score).An analytical threshold evaluation check monitors the results:If the score falls below 90, the row updates on the UI table layout, and the process completes.If the score scales to 90 or above, the system triggers the Automation Agent pipeline to draft a personalized pitch email.Section 5: Phase 5 & Agent 5 (The Corsair Security & Outreach Agent)Pitch Customization StrategyTo ensure your outreach stands out, the Automation Agent generates tailored communication strategies across all lead tiers, not just hot leads.For Hot Leads (Score 90+): The AI builds a highly specialized, direct pitch containing technical battlecards and migration timelines.For Cold Leads (Score < 70): The system generates a low-pressure, educational introductory pitch focused on ecosystem trends, ensuring your brand builds a pipeline with early-stage targets without sounding overly sales-heavy.The Deep UI Layout Trap & App Deep Linking MechanicsThe generated pitch draft is written to a dedicated table database row linked directly to the main lead card container. When a user clicks an item in the dashboard table view, a slide-out drawer or overlay card displays the generated text.The interface includes an active confirmation control labeled Allow / Approve. When clicked, the app runs a frontend deep-linking operation to hand the message off to the user's local communications workflow. It uses a structured format wrapper:TypeScriptconst handleApprove = (email: string, subject: string, body: string) => {
  const mailtoUrl = `mailto:${encodeURIComponent(email)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
  window.location.href = mailtoUrl;
};
When this function runs, it opens the user's native system mail application (such as Microsoft Outlook or the native Windows/macOS Mail app) with the recipient's address, subject line, and the AI-drafted pitch text pre-populated and ready to send.Technical Workflow: Agent 5 (Corsair Security Gateway)[Pitch Draft Generation Complete] ──► Corsair Guard Configuration Checks (cautious)
                                                    │
                                                    ▼
                                      Intercept Execution & Halt Action
                                                    │
                                                    ▼
                                      Write state: 'pending_approval' to DB
                                                    │
                                                    ▼
                                      User clicks [Approve] on Next.js UI
                                                    │
                                                    ▼
                                      Execute Deep Linking Mailto Event Controller
The background service layer completes the message text layout generation step.The workflow tries to execute an email tool call. The operation is caught by the integrated Corsair SDK framework operating under system-wide cautious mode guardrails.Corsair halts the outgoing data transaction, logs a tracking footprint, and marks the item status in the database as pending_approval.The frontend reads this state configuration change and updates the card container on the user interface dashboard layout with an active notification layout flag.The operator reviews the drafted pitch text within the slide-out interface layout container.The user clicks the Approve action controller. This signals Corsair to release the data lock, switches the database status column indicator to authorized, and executes the deep-linking utility to open the message within your native mail client.Section 6: Phase 6 & Agent 6 (The Intelligence Dashboard Agent)The "Ghost Component" Analytics Logic IntegrationYou mentioned you couldn't find this agent inside your current folder repository maps. That is because an analytics engine does not live inside an isolated background service file. It runs as a series of relational database aggregation methods that transform logged interaction rows into visual layout elements on your frontend.The Intelligence Dashboard Agent handles three core metrics:Lead Intelligence Dashboard: A data summary engine that groups leads by intent classification status tags using SQL aggregation operations (GROUP BY intent_stage).Opportunity Heatmaps: A component grid layout that tracks target activity density, highlighting which sectors or industries (e.g., Skincare vs. Lip-care labels) are showing the highest concentration of active buying or hiring signals.Conversion Analytics Pipelines: A step-by-step visual chart showing lead progression states, tracking conversions from initial signal detection through Corsair review passes to final email dispatch actions.Technical Workflow: Agent 6 (Intelligence Dashboard Engine)[Database Activity Log States] ──► FastAPI Aggregation Routes (/api/v1/analytics/summary)
                                                    │
                                                    ▼
                                      SQL Window Functions Processing
                                                    │
                                                    ▼
                                      Return Clean Aggregated JSON Blocks
                                                    │
                                                    ▼
                                      Map JSON to Visual Dashboard Elements
The user opens the main metrics tab panel workspace view on the Next.js frontend console framework.The client app executes an asynchronous fetch command querying your analytics controller route: GET /api/v1/analytics/summary.The FastAPI platform processes the data request by executing window query functions directly across your database tables to compute real-time lead distribution scores.The system calculates trend direction changes by comparing historical weekly performance logs against your current active pipeline data.The database packs the resulting analysis into a clean, optimized JSON payload structure.The backend hands the data block off to your Next.js views. The application takes the raw values and maps them directly into data summary components, interactive tracking graphs, and bento-grid layouts.Section 7: Structural Architecture & Advanced UI States7. The Shared Database Schema (Single Source of Truth)To ensure all six agents coordinate smoothly without data conflicts, they must read from and write to the same central database instance. Here is the structure for your unified data schema:┌────────────────────────────────────────────────────────────────────────┐
│                        CORE RELATIONAL DATABASE                        │
├────────────────────────────────────────────────────────────────────────┤
│  users / tenants                                                       │
│  └── id (UUID) | company_name (TEXT) | industry_type (TEXT)            │
│                                                                        │
│  target_profiles                                                       │
│  └── id (UUID) | org_id (FK) | name (TEXT) | domain_url (TEXT)         │
│                                                                        │
│  harvested_signals                                                     │
│  └── id (UUID) | target_id (FK) | title (TEXT) | body (TEXT)           │
│      └── dedup_hash (TEXT UNIQUE) | created_at (TIMESTAMP)             │
│                                                                        │
│  intent_analysis                                                       │
│  └── signal_id (FK) | score (INT) | stage (TEXT) | pitch_draft (TEXT)  │
│      └── corsair_status (TEXT) ['pending_approval' | 'authorized']     │
└────────────────────────────────────────────────────────────────────────┘
8. Exact Function of Dashboard TabsCommand Center: Your main active operating room. It displays real-time signal ticker feeds on the left, quick simulation tool panels, high-priority notifications, and your Lookalike Discovery feed cards.Pipeline: An enterprise-grade, high-density table view listing your verified target accounts. It allows users to filter, search, and monitor conversion metrics across their entire book of leads.Compliance Ledger: An immutable tracking hub that lists every harvested lead alongside an audit trail proving the data was pulled from compliant, public channels. This provides clear compliance verification if a legal review is ever required.9. The Automation Blueprint Across All StagesAutomation is not just limited to the scoring phase; it drives every step of the pipeline:Signal Ingestion: Celery Beat workers automatically poll public developer APIs and job boards every 15 minutes without any manual interaction required.Intent & Scoring: The backend automatically runs text evaluation prompts and calculates numerical lead quality scores the moment a new signal hits the database.Outreach Pre-Production: The system automatically drafts tailored email pitches and structures the email client deep links without requiring any user copywriting.10. Clicking Fatigue Workaround & Blacklist Ledger MechanicsThe Lookalike Click-Fatigue Fix: Forcing an acquisition lead to click "Add to Pipeline" a thousand times is an anti-pattern. To solve this, your Settings configuration drawer will feature an automation control toggle labeled "Autonomous Account Auto-Onboarding". When enabled, any discovered lookalike lead that scores above a certain threshold (e.g., 85+) bypasses the manual confirmation step and is automatically added to the active pipeline tracking matrix.Handling Bad Leads (The Blacklist Mechanism): If a company acts against your platform rules or is a bad fit, you shouldn't delete their history entirely, as you don't want the discovery engine to accidentally ingest and suggest them again next week. Instead, you add a boolean flag column labeled is_blacklisted to the target_profiles database table. When set to true, the UI filters and hides the company from your active view layers, while the backend preserves their historical logs to prevent the discovery engine from re-processing them in the future.Section 8: Technical Tooling Reference MatrixAgent NameCore Functional ResponsibilityBackend Tech Stack RequirementsFrontend Interface Elements1. Context & DiscoveryParses company goals and discovers target lookalikes.FastAPI, LangChain, Pydantic, Google Custom Search APISetup Context Card, Slide-out Config Drawer2. Signal HarvesterGathers real-time and historical news, jobs, and text posts.Celery, Redis, Feedparser, AsyncHttpClientLiveRadarTicker.tsx, Live Stream Components3. Intent AnalyzerClassifies target activity into hiring, buying, or expansion intents.OpenAI/Gemini API, Structured Function CallingColor-Coded Intent Badges (e.g., Consideration)4. Score CalculatorComputes lead quality from 1–100 using a deterministic matrix formula.Python Math Packages, SQLAlchemy Core FiltersWeighted Numerical Rank Indicators5. Corsair SecurityDrafts pitches and enforces human-in-the-loop email execution locks.Corsair Node Core Framework, Deep Linking Router ModulesIntercept Confirmation Modals, Mailto Callers6. Intelligence DashboardGenerates system aggregation reports, activity heatmaps, and funnel data maps.PostgreSQL Window Query Functions, Next.js Layout ParsersBento Grid Analytics Panels, Funnel Visual Charts

build my website according to this